import 'package:flutter/material.dart';

class SpirtualityPage extends StatelessWidget {
  const SpirtualityPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Spirtuality page'),
    );
  }
}
